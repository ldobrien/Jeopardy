import initialState from './initialState'

const finalJeopardyReducer = (state = initialState.finalJeopardy, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default finalJeopardyReducer;