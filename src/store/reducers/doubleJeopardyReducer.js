import initialState from './initialState'

const doubleJeopardyReducer = (state = initialState.doubleJeopardy, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default doubleJeopardyReducer;